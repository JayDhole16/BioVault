{
  "name": "biovault-contracts",
  "version": "0.1.0",
  "description": "Smart contracts for BioVault",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.19.0",
    "@openzeppelin/contracts": "^4.9.3"
  },
  "dependencies": {}
}
